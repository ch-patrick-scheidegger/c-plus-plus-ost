#include "moduleSort.hpp"
#include "module.hpp"

#include <istream>

auto getModules(std::istream & in) -> std::vector<Module> {
    std::vector<Module> modules{};
    std::string line{};
    while(std::getline(in, line)) {

    }
}

auto ModuleSort::printModuleDependencies(std::ifstream &file) -> void {

}
